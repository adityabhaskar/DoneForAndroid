<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:divider">@color/divider</item>

        <!-- Set the theme to be applied for list item backgrounds (selected/unselected) -->
        <item name="android:activatedBackgroundIndicator">@drawable/task_list_item_background</item>

        <!-- Set the colours for contextual action bar -->
        <item name="actionModeBackground">@color/primary</item>
        <item name="actionModeSplitBackground">@color/primary_dark</item>
        <!-- Set the contextual action bar to be drawn OVER the AppCompat action bar-->
        <item name="windowActionModeOverlay">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.SplashTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="AboutDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:background">@android:color/background_light</item>
    </style>

    <style name="touchable_items">
        <item name="android:paddingTop">@dimen/_8dp_half</item>
        <item name="android:paddingBottom">@dimen/_8dp_half</item>
        <item name="android:minHeight">24dp</item>
        <item name="android:minWidth">24dp</item>
        <!--<item name="android:background">@drawable/touchable_item_background</item>-->
        <item name="android:background">?attr/selectableItemBackground</item>
    </style>

    <style name="nav_menu_text_default">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="nav_subtitle" parent="nav_menu_text_default">
        <item name="android:layout_height">48dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">#8A000000</item>
        <item name="android:paddingLeft">16dp</item>
    </style>

    <style name="divider">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/divider</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="BrandedCheckBox" parent="AppTheme">
        <!--<item name="colorAccent">#60000000</item>-->
        <item name="colorAccent">@color/primary</item>
    </style>

</resources>